
name: run

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]


jobs:

  run_dock:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run a multi-line script
        run: |
          docker build -t output .
